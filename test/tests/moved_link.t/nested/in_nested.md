# Nested

This file references the [test file](./test.txt). It needs to fix the path when this is monolithized.

It also has a [sibling reference](./sibling.txt) and points to [top level resource](../top_resource.json).